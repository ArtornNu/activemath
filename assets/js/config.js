/* ============================================================
   QITEP Interactive Math Course — Site Configuration
   ------------------------------------------------------------
   This is the ONE place you'll ever need to paste a secret/key.
   Right now it's empty on purpose — the site works perfectly
   fine without it, using simple iframe embeds of saved activities.

   The Desmos API key below only powers the OPTIONAL "advanced"
   demo in Module 4 (a hand-coded Desmos calculator built with
   JavaScript instead of a plain link). It is completely free:

     1. Go to https://www.desmos.com/my-api
     2. Sign up / log in and request a key (approval is usually
        quick — it's meant for teachers and developers).
     3. Paste the key between the quotes below.
     4. Reload Module 4 — the advanced demo will switch on
        automatically.

   Never commit a *paid/production* key to a public GitHub repo.
   The free tier key is fine to publish; Desmos rate-limits by
   domain, not by secrecy.
   ============================================================ */

window.SITE_CONFIG = {
  desmosApiKey: "" // <-- paste your free Desmos API key here (optional)
};
